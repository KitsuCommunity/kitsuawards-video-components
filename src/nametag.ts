import { makeProject } from '@motion-canvas/core/lib';

import nametag from 'scenes/nametag?scene';

export default makeProject({
	scenes: [nametag],
	background: false, // switch to false when done
});
